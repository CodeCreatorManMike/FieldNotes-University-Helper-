{
  "name": "fieldnotes",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply fieldnotes --local",
    "db:migrate:remote": "wrangler d1 migrations apply fieldnotes --remote"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
